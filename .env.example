APP_NAME="Sistema de Inventario"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

# Lista CSV de origins (scheme://host[:port]) desde los cuales el backend
# acepta requests autenticados via cookie httpOnly. En produccion deberia
# contener SOLO el origin publico del SaaS. En dev se incluyen los puertos
# comunes de Vite (5173) y del backend mismo (8000). Ver config/app.php.
APP_ALLOWED_ORIGINS_FOR_CSRF=http://localhost,http://localhost:5173,http://localhost:8000,http://127.0.0.1,http://127.0.0.1:5173,http://127.0.0.1:8000,https://app.miinventariofacil.com,https://www.miinventariofacil.com

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5434
DB_DATABASE=inventory_arens
DB_USERNAME=inventory_arens
DB_PASSWORD=secret

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
# Imports grandes se ejecutan en segundo plano y no bloquean la peticion HTTP.
DATA_IMPORT_QUEUE=imports
DATA_IMPORT_MAX_FILE_MB=50
DATA_IMPORT_MAX_ROWS=100000
# Debe superar el timeout del job cuando se usa la cola database.
DB_QUEUE_RETRY_AFTER=3900
REDIS_QUEUE_RETRY_AFTER=3900

# Broadcasting (Reverb / WebSocket push notifications).
# En produccion se cambia BROADCAST_CONNECTION=reverb y se completan las
# claves Reverb generadas con `php artisan reverb:install` (ver docs/
# REVERB_WEBSOCKETS_PLAN.md para el despliegue en VPS).
REVERB_APP_ID=inventarioarens-app
REVERB_APP_KEY=inventarioarens-key
REVERB_APP_SECRET=inventarioarens-secret-change-me
REVERB_HOST=0.0.0.0
REVERB_PORT=8081
REVERB_SCHEME=http

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

SYNC_CLOUD_URL=
SYNC_CLOUD_TOKEN=
# Base publica de la NUBE para construir cloud_url de imagenes de producto.
# En la nube coincide con APP_URL; en los nodos locales apunta a la nube
# (p.ej. https://app.miinventariofacil.com) para que la nube pueda descargar
# las imagenes subidas localmente.
SYNC_PUBLIC_BASE=

CORS_ALLOWED_ORIGINS_LOCAL=http://localhost:8000
CORS_ALLOWED_ORIGINS_DOMAINS=https://app.miinventariofacil.com

# Limite por minuto para cada credencial de integracion CRM.
CRM_RATE_LIMIT_PER_MINUTE=60
