<UserControl x:Class="InventoryDesktop.Modules.InventoryCenter.InventoryMovementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Padding="22" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
            <Grid>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Padding="9,5" Background="#EEF0FF" BorderBrush="#DCE4FF" BorderThickness="1" CornerRadius="7">
                            <TextBlock Text="MOVIMIENTOS" Foreground="{StaticResource AccentStrongBrush}" FontSize="10" FontWeight="Black" />
                        </Border>
                        <Border Margin="8,0,0,0" Padding="9,5" Background="#F8FAFE" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="7">
                            <TextBlock Text="{Binding PaginationLabel}" Foreground="{StaticResource MutedBrush}" FontSize="10" FontWeight="Black" />
                        </Border>
                    </StackPanel>
                    <TextBlock Text="Entradas y salidas de producto" FontSize="27" FontWeight="Black" Margin="0,12,0,0" />
                    <TextBlock Text="Busca un producto y registra una entrada o salida usando las reglas del backend."
                               Foreground="{StaticResource MutedBrush}"
                               FontWeight="SemiBold"
                               TextWrapping="Wrap"
                               Margin="0,6,0,0" />
                </StackPanel>

                <Button Content="Actualizar"
                        Width="118"
                        Height="38"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        FontWeight="Black"
                        Click="Refresh_Click" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Padding="14" Margin="0,14,0,14" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Buscar producto" Foreground="{StaticResource MutedBrush}" FontWeight="Black" FontSize="12" />
                    <TextBox Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,5,14,0" />
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Buscar"
                        Width="105"
                        Height="38"
                        Margin="0,17,10,0"
                        FontWeight="Black"
                        Background="{StaticResource AccentBrush}"
                        Foreground="White"
                        Click="ApplyFilters_Click" />
                <Button Grid.Column="2"
                        Content="Limpiar"
                        Width="105"
                        Height="38"
                        Margin="0,17,0,0"
                        FontWeight="Black"
                        Click="ClearFilters_Click" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
            <DataGrid ItemsSource="{Binding Products}"
                      SelectedItem="{Binding SelectedProduct}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      RowHeight="58"
                      Margin="0">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="PRODUCTO" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Margin="10,0">
                                    <TextBlock Text="{Binding Name}" FontWeight="Black" FontSize="14" />
                                    <TextBlock Text="{Binding Sku}" Foreground="{StaticResource MutedBrush}" FontWeight="Bold" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="CONTROL" Binding="{Binding TrackingLabel, Mode=OneWay}" Width="150" />
                    <DataGridTextColumn Header="DISP." Binding="{Binding Stock.Available, Mode=OneWay}" Width="80" />
                    <DataGridTextColumn Header="RES." Binding="{Binding Stock.Reserved, Mode=OneWay}" Width="80" />
                    <DataGridTemplateColumn Header="ACCIONES" Width="230">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Entrada" Width="92" Height="34" FontWeight="Black" Click="Entry_Click" />
                                    <Button Content="Salida" Width="92" Height="34" Margin="8,0,0,0" FontWeight="Black" Click="Exit_Click" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusMessage}" Foreground="{Binding StatusBrush}" FontWeight="Bold" TextWrapping="Wrap" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="Anterior" Width="105" Height="38" FontWeight="Black" IsEnabled="{Binding CanGoPrevious}" Click="PreviousPage_Click" />
            <Button Grid.Column="2" Content="Siguiente" Width="105" Height="38" Margin="10,0,0,0" FontWeight="Black" IsEnabled="{Binding CanGoNext}" Click="NextPage_Click" />
        </Grid>
    </Grid>
</UserControl>

